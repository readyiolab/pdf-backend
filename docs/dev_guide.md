# Developer Documentation Guide

This document is a reference guide for developers contributing to the PDF Tools SaaS project. It outlines the technology stack, directory layout, database table prefixes, and integration steps.

---

## 1. Project Overview & Decoupled Architecture

This project is built as a highly scalable monorepo hosting two independent services that do not share runtime loops:
* **Express API Service (`api/`)**: Handles public traffic, user authentication, file upload coordinates, job requests, Razorpay webhooks, and billing statuses.
* **Worker Service (`worker/`)**: Listens to Redis BullMQ queues (`heavy-jobs` and `light-jobs`) and processes CPU/memory-intensive file conversions (OCR, compression, splits, merges) in isolation.

Both services share configuration limits and typescript definitions through the [shared/](file:///d:/Arun%20sir%20Projects/PDFPRODUCT/backend/shared) module, ensuring a single source of truth for features and rules.

---

## 2. Database Design & Table Prefixes

To prevent conflicts and follow enterprise styling, all database tables are prefixed with `tbl_`. 

The database connections are managed via a raw connection pool using `mysql2/promise` located at [api/src/lib/mysql.ts](file:///d:/Arun%20sir%20Projects/PDFPRODUCT/backend/api/src/lib/mysql.ts) and [worker/src/lib/mysql.ts](file:///d:/Arun%20sir%20Projects/PDFPRODUCT/backend/worker/src/lib/mysql.ts).

### Auto-Initialization DDL Schema

When the Express API boots up, it automatically verifies and runs DDL queries to create the following three tables if they do not exist:

#### `tbl_user`
Stores user profile information, pricing plans, and daily operation limits.
* `id` (VARCHAR(255), PK): UUID generated by the API on registration.
* `email` (VARCHAR(255), Unique): Hashed lowercase email.
* `passwordHash` (VARCHAR(255)): Password hashed using Bcrypt.
* `name` (VARCHAR(255), Nullable): Full name of the user.
* `plan` (VARCHAR(50)): Default is `'FREE'` (max 5 operations/day). Can be upgraded to `'PRO'`.
* `dailyOpsUsed` (INT): Number of operations processed during the current day.
* `dailyOpsResetAt` (DATETIME): Timestamp when the daily operation counter resets (after 24 hours).
* `createdAt` (DATETIME): User sign up timestamp.

#### `tbl_job`
Tracks active, queued, completed, and failed file conversion tasks.
* `id` (VARCHAR(255), PK): UUID matching the BullMQ job identifier.
* `userId` (VARCHAR(255), FK): Reference to `tbl_user(id)`.
* `tool` (VARCHAR(100)): Name of the utility (e.g. `'merge'`, `'compress'`, `'ocr'`).
* `status` (VARCHAR(50)): Enum state (`'QUEUED'`, `'PROCESSING'`, `'COMPLETED'`, `'FAILED'`).
* `inputFiles` (JSON): Array of S3 keys uploaded by the client.
* `outputFile` (VARCHAR(255), Nullable): S3 key of the conversion result.
* `errorMessage` (TEXT, Nullable): Failure stack trace if status is `FAILED`.
* `createdAt` (DATETIME): Job request timestamp.
* `completedAt` (DATETIME, Nullable): Job completion timestamp.
* `expiresAt` (DATETIME): Cleanup timestamp (1 hour after creation).

#### `tbl_subscription`
Stores active Razorpay checkout subscription logs.
* `id` (VARCHAR(255), PK): System identifier.
* `userId` (VARCHAR(255), FK): Reference to `tbl_user(id)` (cascade deletes).
* `razorpaySubId` (VARCHAR(255), Nullable): Razorpay subscription identifier (`sub_X1234abc`).
* `status` (VARCHAR(100)): Subscription status from webhooks (e.g. `'active'`, `'cancelled'`).
* `currentPeriodEnd` (DATETIME, Nullable): Current billing cycle end date.

---

## 3. Asynchronous Queue Processing

The API service dispatches jobs to the BullMQ system:
1. **Light Queue (`light-jobs`)**: Handles quick executions like splitting, merging, rotating, watermarking, and protecting PDFs. Runs with up to **10 concurrent workers**.
2. **Heavy Queue (`heavy-jobs`)**: Handles CPU-bound jobs like Office file conversions, Ghostscript compressions, and Tesseract OCR. Runs with a max concurrency of **2 workers** to prevent resource starvation.

---

## 4. DigitalOcean Spaces Folder Structure

To ensure that objects in the Space remain organized, descriptive, and easily understandable for administrators, file objects are saved inside named subdirectories:

1. **User Uploads**:
   - Path format: `pdf-saas-uploads/user-${userId}/${uniqueUUID}_${sanitizedFileName}`
   - *Example*: `pdf-saas-uploads/user-mock-user-id/a5b6c7d8_report.pdf`
2. **Processed Results**:
   - Path format: `pdf-saas-results/job-${jobId}/${toolName}_${timestamp}.${extension}`
   - *Example*: `pdf-saas-results/job-mock-job-id/merged_1719744300000.pdf`

---

## 5. Local Development Walkthrough

1. Set up your local `.env` variables following the templates in `.env.example`.
2. Boot up Redis:
   ```bash
   docker compose up -d
   ```
3. Run DDL tables checks and start the development servers:
   ```bash
   npm run dev
   ```
